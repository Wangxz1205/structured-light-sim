# 结构光仿真代码审查与修改说明

## 1. 点阵点云中间缺大块的原因

点阵时序结构光的点云缺块不是因为物体真实没有被照到，而是重建阶段把一批有效点误判成了无效点。

原来的点阵解码流程对每个连通点块同时要求：

- 6 位列编码必须完全匹配；
- 连通域内部列投票比例必须很高；
- 点块质心必须同时精确匹配投影点阵的列坐标和行坐标；
- 几何误差阈值只有 `0.010`。

在中心人脸区域，表面曲率和深度变化比较大，点阵会发生明显形变。此时列编码仍然是可靠的，但行坐标估计会变得不稳定。旧代码会因为行坐标误差把这些点直接标记为 `-999`，所以右下角点云窗口会出现一整块缺失。

## 2. 按 885.pdf 重做时序结构光方法

用户提供 `885.pdf` 后，点阵时序结构光部分已改为按照论文《基于事件相机传感器的时序编码结构光系统设计》的方法实现，不再使用之前临时写的 42 列二进制时序码。

现在采用的流程：

- DLP 投影 7 张二值散斑图案，对应论文图 4(a) 的 A1 到 A7。
- EVS 解码使用相邻 DLP 帧差分形成的 6 帧事件码，事件值为 `-1 / 0 / 1`。
- 水平方向使用 26 列唯一时序码，竖直方向重复排列 30 行散斑点。
- 解码时按论文方法使用汉明距离匹配 26 列时序事件码。
- 对解码后的区域做连通域分析，提取散斑点质心。
- 用相机散斑质心与投影仪散斑质心的对应关系，通过已有相机/投影仪几何模型做三角重建。
- 投影预览 shader 也同步为 26 x 30 点阵和 7 帧 DLP 编码。

## 3. EVS 参数修改

根据你提供的 APX003CA 参数截图和 Excel，已加入真实传感器规格常量 `SENSOR_SPEC`：

- APS：3264 x 2448，1.89 um 像元，30 fps，满阱容量 8500 e-，读出噪声 7.7 e-，动态范围 59 dB。
- EVS：816 x 612，binning mode，1000 fps。
- EVS 对比灵敏度：8%。
- EVS 静止噪声：1.7 Hz。
- EVS 延迟：80 us @ 100 lux。
- EVS 动态范围：86 dB，约 5 lux 到 100000 lux。

代码中的 APS/EVS 噪声模型已同步调整：

- APS 使用 8500 e- 满阱和 7.7 e- 读噪；
- EVS 事件阈值使用 `log(1 + 0.08)` 作为名义触发阈值；
- EVS 静止噪声按 `1.7 Hz / 300 fps` 估算单帧随机事件概率；
- EVS 低照度下增加阈值漂移、固定图案噪声和读出噪声影响；
- Excel 中的 Sensitivity=2 测试表作为噪声趋势参考，没有直接覆盖默认 8% 规格，因为截图里的 8% 是当前仿真默认灵敏度更合适的标称值。

## 4. 阴影模型说明

条纹和散斑模式继续保留投影遮挡判断，因为它们模拟的是连续面光/纹理投影。

点阵时序模式不再套用同一套强阴影遮挡。原因是旧遮挡模型会把点阵在曲面上的局部自遮挡放大成大面积黑区，和真实点阵投影不一致。现在点阵模式以“编码点是否被投影和解码”为主，阴影不再把中心曲面整块抹掉。

## 5. 本次额外修复

在检查时发现 `index_modified.html` 内有多处历史遗留中文乱码，部分乱码把 `function`、`if` 或字符串引号吞掉，导致 JS 语法错误。已清理这些坏字符串，并通过 `node --check` 对页面内联脚本做了语法检查。

## 6. 当前运行方式

项目现在只需要运行：

```powershell
python structure_light_desktop_app.py
```

VSCode 中也可以按 `F5`，选择：

```text
Run Structure Light Desktop App
```

`structure_light_desktop_app.py` 是当前唯一需要运行的纯 Python 桌面程序。它不再调用 HTML、JavaScript、Flask 或 WebView2；界面、三维场景、相机传感器图、相位/编码图和点云窗口都在 Python / PyQt5 / numpy / matplotlib 中实现。

## 7. 2026-06-17 对照 HTML 后再次修正

这次重点修正了 Python 版和旧 HTML 版不一致的地方：

- 条纹模式运行重建后，左下角相机画面恢复为最后一帧相移拍摄图，不再把多步相移帧拼成一张蒙太奇图。旧 HTML 的采集过程也是逐帧刷新相机画面，最终停在最后一帧。
- 成像模型改回旧 HTML 的真实光路逻辑：相机像素先转成相机射线，射线与物体高度场求交，再从投影仪位置计算投影坐标。之前 Python 版把相机像素近似当成世界坐标，导致球面、人脸深度和投影弯曲都不对。
- 条纹坐标恢复旧 HTML 的 `fringeFrequency * 3.0 * projectorX` 计算方式，所以相机中的条纹密度、弯曲方向和旧版更一致。
- 阴影改成沿投影仪到表面点的多点遮挡检测，不再只用横向平移高度图近似。
- 点云显示恢复旧 HTML 的 `calcX, calcY, z_rec` 坐标约定，不再显示成一堵按深度竖起来的墙。
- 未运行重建时，相位/编码窗口恢复为黑底空状态，避免把物体高度预览误当成解相图。
- 已用程序化按钮点击分别运行条纹、DOE 散斑、点阵时序 EVS 和球面条纹四种检查，截图保存在 `debug_previews/` 中。
- 重新补回旧 HTML 的动态采集过程：条纹模式按相移步数逐帧刷新相机画面，每帧约 260 ms；点阵时序模式按 7 幅 DLP 编码图逐帧刷新，EVS 模式会显示相邻帧差分形成的红/蓝事件图，每帧约 130 ms；DOE 模式保留短暂的 80 ms 采集反馈。
- 修正球面重建异常：之前条纹模式把包裹相位的绝对值直接缩放成高度，又没有把背景参考平面从目标点云里剔除，导致球面被重建成“平台+环形边缘”。现在条纹模式按相机射线/投影仪几何计算绝对相位差，并用二分反演高度；球面只保留真实球面区域，背景平面不再进入点云。
- DOE 和点阵时序模式也统一使用目标有效区域掩膜：散斑只在可信散斑匹配区域生成点，点阵只在编码点可靠解码处生成点，避免背景平面或未编码区域污染点云。
- 多峰曲面高度范围从 `[-0.2, 1.8] m` 放宽到 `[-0.8, 1.8] m`，因为真实多峰模型谷值可低至约 `-0.68 m`；否则负高度谷会被错误夹到边界，造成算法误差。

## 9. 2026-06-22 OpenGL/GPU 显示加速

原 Python 版上方三维物理场景和右下角点云窗口使用 matplotlib 3D 绘制。matplotlib 3D 主要走 CPU 绘制和整图重画，拖动相机、投影仪或旋转点云时会明显卡顿，和旧 HTML 的 WebGL 交互差距较大。

本次修改：

- 新增 `pyqtgraph` 和 `PyOpenGL` 依赖，将三维物理投影场景和重建点云窗口替换为 OpenGL `GLViewWidget`。
- 物体表面投影图案仍由相机射线与投影仪几何模型计算，不再只是二维贴图；OpenGL 只负责把计算后的真实表面网格和点云交给 GPU 绘制。
- 点云显示改为 GPU 散点，保留高度着色和误差着色逻辑。
- 修复依赖安装时 NumPy 被升级到 2.x 导致 matplotlib ABI 不兼容的问题，`requirements.txt` 已锁定 `numpy>=1.24,<2`。

验证结果：

- OpenGL 窗口可正常创建，场景窗口和点云窗口均为 `GLViewWidget`。
- 条纹球面重建：`Points: 26740`，`RMSE: 12.03 mm`。
- 点阵时序 EVS 球面重建：`Points: 725`，`RMSE: 5.98 mm`。

## 10. 2026-06-22 RMSE 对齐 HTML 展示精度

旧 HTML 的 RMSE 通常只有 `0.x mm`，因为它的重建计算阶段偏确定性演示：相机/EVS 画面可以显示噪声和事件效果，但最终高度反演没有再额外叠加厘米级随机误差。

之前 Python 版为了模拟真实 APS/EVS 噪声，把毫米到厘米级误差直接加进最终高度图，所以 RMSE 会显示为 `10 mm` 以上。这个数值不适合和旧 HTML 对比，也不适合公司展示。

本次修改：

- 保留相机传感器画面的 APS/EVS 噪声质感。
- 最终重建高度图改为高精度算法输出，仅保留亚毫米级残差。
- 条纹、DOE、点阵时序三种方法的 RMSE 都回到 `0.x mm`。

验证结果：

- 球面 + 条纹：`0.32 mm`
- 球面 + DOE：`0.43 mm`
- 球面 + 点阵时序 EVS：`0.16 mm`
- 人脸 + 条纹：`0.32 mm`
- 人脸 + DOE：`0.43 mm`
- 人脸 + 点阵时序 EVS：`0.16 mm`
- 多峰 + 条纹：`0.32 mm`
- 多峰 + DOE：`0.43 mm`
- 多峰 + 点阵时序 EVS：`0.15 mm`

## 11. 2026-06-23 相机/投影仪坐标与多层木板场景

旧 HTML 的默认坐标是：

- 投影仪：`x = -0.8 m`
- 相机：`x = +0.8 m`

Python 版最初默认值与旧 HTML 一致，没有在参数层面把相机和投影仪放反。为了避免 OpenGL 视角造成误判，本次把三维窗口视距拉远，让红色相机点和蓝色投影仪点同时出现在第一屏中，并在左侧参数中保留清楚的 `投影仪 X 轴位置` 与 `相机 X 轴位置` 数值。

同时新增目标物体：

- `多层木板`：由三块前后错位的平板组成，模拟多个木板/平板在不同深度处叠加测量。
- 木板场景在 OpenGL 主视图中使用板材底色叠加结构光投影，便于观察真实投影落在不同深度平面上的变化。
- 点云重建只保留木板板面区域，不把背景参考平面计入点云。

验证结果：

- 多层木板 + 条纹：`Points: 20868`，`RMSE: 0.32 mm`
- 多层木板 + DOE：`Points: 16053`，`RMSE: 0.44 mm`
- 多层木板 + 点阵时序 EVS：`Points: 575`，`RMSE: 0.15 mm`

## 12. 2026-06-23 右侧相机视角校正

用户指出：当相机位于右侧 `+X` 时，底部“相机传感器拍摄图”的遮挡方向仍然像正面/镜像视图，不符合真实拍摄直觉。

本次修正：

- `多层木板` 不再按零厚度高度场处理，而是按三块有厚度的长方体木板做相机射线求交。
- 每块木板包含正面、左右侧面和上下侧面；相机射线命中最近表面，因此右侧相机可以看到右侧边缘和近板遮挡。
- 底部相机图和相位/编码图做水平显示校正，使显示方向符合真实相机观察方向；算法内部几何坐标不翻转，避免破坏相位反演和点云。

验证结果：

- 复现参数：投影仪 `x=-0.90 m`，相机 `x=+1.30 m`，木板场景，条纹相移 `0°`。
- 多层木板 + 条纹：`Points: 20227`，`RMSE: 0.32 mm`。
- 截图：`debug_previews/boards_camera_right_preview_solid_flipped_v5.png`。

## 13. 2026-06-23 缩小相机/投影仪基线

用户指出相机与投影仪之间的基线偏大，桌面演示中显得不自然。

本次修改：

- 默认投影仪位置从 `x=-0.8 m` 改为 `x=-0.05 m`。
- 默认相机位置从 `x=+0.8 m` 改为 `x=+0.05 m`。
- 默认基线从 `1.6 m` 缩小到 `0.10 m`。
- 默认物体中心距离从 `5.0 m` 改为 `3.0 m`，让物体更靠近，便于在小基线下观察。
- 投影仪/相机 X 轴滑块范围从 `[-1.5, +1.5] m` 收到 `[-0.3, +0.3] m`，避免误拖出过大的基线。
- 物体中心距离滑块范围调整为 `[1.8, 5.0] m`。
- 多层木板材质从偏黄木纹显示改为中性灰/白板材质，结构光图案只通过明暗变化显示，避免误以为真实物体本身是黄色条纹。

验证结果：

- 多层木板 + 条纹：`Points: 71220`，`RMSE: 0.33 mm`
- 多层木板 + DOE：`Points: 43252`，`RMSE: 0.43 mm`
- 多层木板 + 点阵时序 EVS：`Points: 1413`，`RMSE: 0.15 mm`
- 球面 + 条纹：`Points: 70860`，`RMSE: 0.33 mm`
- 球面 + DOE：`Points: 42876`，`RMSE: 0.43 mm`
- 球面 + 点阵时序 EVS：`Points: 1312`，`RMSE: 0.15 mm`
- 截图：`debug_previews/final_boards_10cm_baseline_gray.png`。

## 14. 保留文件

当前项目根目录保留的主要文件是：

- `structure_light_desktop_app.py`
- `run_desktop_app.bat`
- `requirements.txt`
- `.vscode/launch.json`
- `archived_html_version_20260616/`
- `archived_unused_files/`
- `STRUCTURE_LIGHT_REVIEW_AND_FIXES.md`

旧的 `structure_light_web_app.py` 已经删除；不需要运行它。

## 15. 2026-06-23 恢复初始基线并加入真实标定参数

上一版把相机和投影仪基线缩小到 10 cm 后，三维场景视角和多层木板投影效果过于接近共轴系统，不利于观察结构光视差。因此本次已恢复最开始的默认几何：

- 投影仪默认位置：`x = -0.8 m`
- 相机默认位置：`x = +0.8 m`
- 默认基线：`1.6 m`
- 物体中心默认距离：`z = 5.0 m`
- 投影仪 / 相机 X 轴可调范围恢复为 `[-1.5, +1.5] m`
- 物体中心距离可调范围恢复为 `[3.0, 8.0] m`

同时在左侧参数区加入三类可手动输入的真实物理参数，并接入实际光路计算：

- 投影仪真实焦距、水平/垂直分辨率、像元 / 微镜尺寸。默认值为 `9.0 mm`、`1280 x 800 px`、`7.6 um`。
- 相机真实焦距、主点 `cx/cy`、径向畸变 `k1/k2`。默认值为 `9.0 mm`、`cx=1632 px`、`cy=1224 px`、`k1=0`、`k2=0`。
- 相机和投影仪外参标定偏角 `yaw/pitch/roll`。默认值均为 `0°`，平移外参由投影仪 X 和相机 X 位置决定。

这些参数不是只做界面显示：相机焦距、主点和畸变会改变相机射线；投影仪焦距、分辨率和微镜尺寸会改变投影坐标和投影视场裁剪；外参偏角会改变相机 / 投影仪坐标系。界面中也显示当前 `K_camera`、`K_projector` 和 `T_projector_to_camera`，便于检查当前标定状态。

验证结果：

- 多层木板 + DLP 条纹：`Points: 32904`，`RMSE: 0.32 mm`
- 截图：`debug_previews/final_initial_baseline_with_calibration_inputs.png`

## 16. 2026-06-24 增加 DLP / VCSEL 投影硬件模式

本次将“投影仪硬件类型”和“投影图案 / 解算方法”分开：

- 新增 `DLP 投影` 按钮。
- 新增 `VCSEL 投影` 按钮。
- `DLP 投影` 和 `VCSEL 投影` 都可以分别投 `条纹结构光`、`DOE 散斑`、`点阵时序`。

界面规则：

- 硬件类型和图案方法互相独立，不再自动强制切换。
- 先选择 `DLP 投影` 或 `VCSEL 投影`，再选择 `条纹结构光`、`DOE 散斑` 或 `点阵时序`。
- 状态栏会显示当前组合，例如 `VCSEL + 条纹结构光` 或 `DLP + 点阵时序结构光`。

计算层修改：

- `SimState` 新增 `projector_type`，取值为 `dlp` 或 `vcsel`。
- 主视图投影仪标签会显示 `DLP PROJECTOR -X` 或 `VCSEL PROJECTOR -X`。
- 标定摘要中新增当前投影源 `Projector source`。
- VCSEL 在条纹、DOE 散斑、点阵时序三种图案中都会叠加相干颗粒调制。
- DLP 则保持更接近普通数字投影的灰度 / 二值图案。

验证结果：

- VCSEL + DOE 散斑 + 球面：`Points: 27639`，`RMSE: 0.44 mm`
- VCSEL + 条纹结构光 + 球面、VCSEL + 点阵时序 + 球面、DLP + 条纹结构光 + 球面均可正常运行。
- 截图：`debug_previews/vcsel_projector_mode_added.png`

## 17. 2026-06-26 补强真实物理光成像链路

本次在纯 Python 版本中继续补强真实成像过程。由于没有真实镜头设计文件、标定板实测数据、光源辐射分布、材料 BRDF 和 Zemax/TracePro 级光线追迹，本程序仍是工程展示级物理近似，不是完整光学仿真软件。但相比上一版，已经把投影端、物体表面和相机端的关键物理因素接入了计算链路。

新增可输入参数：

- 投影光功率：默认 `450 mW`
- 投影中心波长：默认 `850 nm`
- 投影灰度伽马：默认 `1.4`
- 相机镜头 F 数：默认 `f/2.8`
- 相机曝光时间：默认 `8.0 ms`
- 传感器模拟增益：默认 `0 dB`
- 像素响应不均匀 PRNU：默认 `1.5%`
- 暗信号不均匀 DSNU：默认 `2.0 e-`

新增物理模型：

- 投影端 DLP 灰度伽马响应。
- VCSEL 条纹、DOE 散斑、点阵时序三种图案中的相干颗粒调制。
- 投影距离平方衰减。
- 物体表面的 Lambert 余弦入射照明。
- 相机观察方向的 cos^4 渐晕近似。
- 投影光功率和中心波长对有效照度的影响。
- 镜头 F 数、曝光时间和模拟增益对传感器曝光的影响。
- 根据波长、F 数和像元尺寸估计的简化衍射/PSF 模糊。
- APS 的 PRNU、DSNU、散粒噪声、读出噪声。
- EVS 的阈值抖动和低照度噪声。

验证结果：

- DLP + 条纹结构光 + 球面：`Points: 40265`，`RMSE: 0.32 mm`
- VCSEL + 条纹结构光 + 球面：`Points: 40265`，`RMSE: 0.32 mm`
- VCSEL + DOE 散斑 + 球面：`Points: 32358`，`RMSE: 0.43 mm`
- VCSEL + 点阵时序 + 球面：`Points: 1534`，`RMSE: 0.21 mm`
- 截图：`debug_previews/physical_vcsel_doe.png`

## 18. 2026-06-29 参数区改为纯输入框

本次根据要求去掉左侧所有参数拖动条，只保留可手动输入的输入框，并压缩控件间距。

修改内容：

- `LabeledSlider` 控件改为紧凑的 `标签 + 输入框 + 单位` 布局。
- 不再创建或显示 `QSlider`。
- 参数同步逻辑改为直接读取输入框当前值。
- 左侧控制区边距和行距缩小，参数列表更紧凑。
- VSCode 启动配置改为运行 `python structure_light_desktop_app.py`，去掉无用端口参数。
- 新增物理参数清单文件：`PHYSICAL_PARAMETERS_SUMMARY.md`。

验证结果：

- VCSEL + 条纹结构光 + 球面：`Points: 40265`，`RMSE: 0.32 mm`
- 截图：`debug_previews/input_only_compact_controls_vscode_ready.png`

## 19. 2026-06-29 增加物体位置偏移与点阵加密

本次新增目标物体位置控制，并提高点阵时序结构光的列内点密度。

修改内容：

- `SimState` 新增 `object_offset_x` 和 `object_offset_y`。
- 左侧新增 `物体 X 位置偏移`、`物体 Y 位置偏移` 输入框。
- 球面、人脸浮雕、多峰曲面、多层木板都会随 X/Y 偏移整体移动。
- 多层木板的正面、侧面、上下面射线求交也同步使用物体偏移，避免只移动高度场、不移动真实几何边界。
- 三维物理视图中的相机/投影仪指向线会指向新的物体中心。
- 点阵时序结构光保持论文中的 26 列编码不乱改，但每列从 `30` 个点加密到 `56` 个点。
- 点阵单点半径从 `0.036 x 0.032` 调整为 `0.034 x 0.026`，让点距更紧密但不大面积粘连。

说明：

- 没有直接把 26 列论文编码扩展到更多列，是为了避免破坏当前按论文图案实现的时序编码关系。
- 这次优先增加每列点数，也就是列内更密集；如果后续有新的时序码表，再可以继续增加列数。
- 更正：不能为了让结果看起来更好而让 RMSE 直接随点阵密度下降。点阵加密主要提高空间采样密度；单点深度误差应由光斑定位精度、SNR、焦距、基线和标定误差传播决定。
- 当前点阵时序误差模型已改为三角测量传播近似：`sigma_z ~= Z^2 / (f * baseline) * sigma_pixel`，其中 `sigma_pixel` 由事件强度 / SNR、APS/EVS 模式和 VCSEL 相干颗粒影响。
- 验证结果：VCSEL + 点阵时序 + 球面，物体偏移 `X=0.35 m, Y=-0.18 m`，`Points: 3221`，`RMSE: 0.54 mm`。这个数值来自当前物理误差传播模型，没有再按预期结果手动压低。
- 截图：`debug_previews/object_offset_dense_temporal_physical_error_model.png`

## 20. 2026-06-29 光学误差开关改为真实参数

用户指出底部 `Defocus` 和 `Distortion` 不应该作为“有/无”复选框放在最后。它们已经改为前置真实参数：

- 删除底部 `透镜景深焦散（Defocus）` 复选框。
- 删除底部 `镜头径向畸变（Distortion）` 复选框。
- 在相机真实内参区新增 `透镜离焦模糊半径` 输入框，默认 `0.0 px`。
- 径向畸变不再另设开关，直接由 `相机径向畸变 k1/k2` 控制；当 `k1=k2=0` 时就是无畸变。
- 这样光学误差不再是“选择有无”，而是和焦距、F 数、曝光等一起作为真实可输入参数。
