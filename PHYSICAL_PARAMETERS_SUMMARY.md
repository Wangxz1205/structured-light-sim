# 结构光仿真物理参数清单

## 已经参与计算的真实物理参数

### 投影仪 / 光源

- 投影仪 X 轴位置：默认 `-0.8 m`
- 投影仪真实焦距：默认 `9.0 mm`
- 投影仪水平分辨率：默认 `1280 px`
- 投影仪垂直分辨率：默认 `800 px`
- 投影仪像元 / 微镜尺寸：默认 `7.6 um`
- 投影光功率：默认 `450 mW`
- 投影中心波长：默认 `850 nm`
- 投影灰度伽马：默认 `1.4`
- 投影硬件绑定：`DLP` 固定用于条纹结构光，`VCSEL` 固定用于点阵时序结构光
- 投影图案：DLP 正弦条纹结构光、VCSEL 点阵时序结构光
- VCSEL 驱动板电路图已确认：`12` 路 VCSEL、`VDD_5V` 主供电、`VBATT -> 5V Boost`、`1 uH` Boost 电感、`10 uF` 输出电容、`PD+/PD-` 监测接口；详细接入建议见 `VCSEL_DRIVER_PARAMETER_INTEGRATION.md`

### 相机 / 传感器

- 相机 X 轴位置：默认 `+0.8 m`
- 相机真实焦距：默认 `9.0 mm`
- 相机镜头 F 数：默认 `f/2.8`
- 相机曝光时间：默认 `8.0 ms`
- 传感器模拟增益：默认 `0 dB`
- 相机主点：默认 `cx=1632 px`、`cy=1224 px`
- 相机径向畸变：默认 `k1=0`、`k2=0`
- 透镜离焦模糊半径：默认 `0.0 px`
- APS 分辨率：`3264 x 2448`
- APS 像元尺寸：`1.89 um`
- APS 满阱容量：`8500 e-`
- APS 读出噪声：`7.7 e-`
- APS 动态范围：`59 dB`
- EVS 分辨率：`816 x 612`
- EVS 帧率：`1000 fps`
- EVS 对比灵敏度：`8%`
- EVS 静止噪声：`1.7 Hz`
- EVS 延迟：`80 us @ 100 lux`
- EVS 动态范围：`86 dB`
- EVS 工作照度范围：`5 lux ~ 100000 lux`
- PRNU：默认 `1.5%`
- DSNU：默认 `2.0 e-`

### 系统几何 / 外参

- 默认基线：`1.6 m`
- 物体中心距离：默认 `5.0 m`
- 物体 X 位置偏移：默认 `0.0 m`
- 物体 Y 位置偏移：默认 `0.0 m`
- 外参 yaw：默认 `0 deg`
- 外参 pitch：默认 `0 deg`
- 外参 roll：默认 `0 deg`
- 投影仪到相机平移：由投影仪 X 和相机 X 自动计算

### 已经建模的物理效应

- 相机射线和投影仪射线几何成像
- 球面、人脸、多峰曲面、多层木板的整体 X/Y 位置移动
- 投影视场裁剪
- 投影距离平方衰减
- Lambert 余弦入射照明
- 相机观察方向 `cos^4` 渐晕近似
- DLP 条纹投影灰度伽马响应
- VCSEL 点阵时序投影相干颗粒调制
- 简化衍射 / PSF 模糊
- 透镜离焦模糊：由“透镜离焦模糊半径”数值控制，不再使用开关
- 径向畸变：由 `k1/k2` 数值控制，`k1=k2=0` 时等价于无径向畸变
- 阴影遮挡
- APS 散粒噪声、读出噪声、PRNU、DSNU
- EVS 阈值抖动、低照度噪声、事件增强显示
- 点阵时序结构光使用 26 列论文编码，列内点数已加密到 56 行

## 还没有接入或仍是简化近似的物理参数

- 投影仪真实光强角分布、光束发散角、均匀性曲线
- DLP 微镜开关时序、PWM 占空比、光源调制带宽
- VCSEL 阵列真实排布、单点功率差异、偏振状态、模式竞争
- VCSEL 驱动板 Boost 芯片型号、最大输出电流、开关频率、输出纹波、MOSFET 型号和开关边沿时间
- VCSEL 点阵真实阵列发散角、单点准直误差和光斑椭圆度
- 镜头真实 MTF 曲线、像散、场曲、彗差、色差
- 完整畸变模型：切向畸变 `p1/p2`、薄棱镜畸变等
- 完整 6DoF 平移外参：目前只开放 X 基线，未开放 Y/Z 平移输入
- 相机光谱响应曲线、量子效率 QE、微透镜效率
- 真实曝光控制、黑电平、ADC 位深、量化噪声、饱和/溢出模型
- APS 暗电流随温度变化模型
- EVS refractory period、带宽限制、事件时间戳抖动、像素级阈值分布
- 材料 BRDF、非 Lambert 反射、高光、半透明和多次反射
- 全局光照、环境反射、互反射
- 真实标定文件导入：例如 OpenCV `yaml/xml/json` 内外参
- Zemax/TracePro 级别的逐光线光学系统仿真

## 暂未完整接入的原因

- 缺少实测参数：例如真实 VCSEL 阵列排布、镜头 MTF、光强角分布、材料 BRDF、相机 QE 曲线等，这些无法仅凭规格表可靠推断。
- 需要专用标定文件：完整 6DoF 外参、切向畸变、薄棱镜畸变、真实主点偏差等最好从标定板和 OpenCV/Zemax 数据导入，手写默认值会让结果看似“精确”但其实不可信。
- 计算量会显著增加：逐光线全局光照、多次反射、真实 VCSEL 点扩散统计、热噪声随时间积分等会让交互界面明显变卡，不适合当前这个需要实时拖动和快速展示的 PyQt/OpenGL 程序。
- 部分模型会互相耦合：例如曝光、增益、QE、光源功率、表面反射率和镜头透过率需要一起标定；单独加一个参数反而容易产生错误物理解释。
- 需要真实硬件确认：EVS refractory period、事件时间戳抖动、像素级阈值分布等通常要根据芯片寄存器设置和实测事件流拟合，不能只靠公开截图确定。
