# VCSEL 驱动板真实物理参数接入说明

## 已从当前文件夹中的电路图确认的信息

来源文件：`12ch_22026-06-26_09-15-41.pdf`

该 PDF 是一张图片式电路图，无法直接提取文字；已从 PDF 中提取原始 JPG 并查看局部放大图。

可以确定的板级信息：

- VCSEL 输出通道数：`12` 路，接口标号为 `VCSEL1` 到 `VCSEL12`。
- VCSEL 主供电：`VDD_5V`。
- 板上存在 `5V Boost`，输入为 `VBATT`，输出为 `VDD_5V`。
- 5V Boost 电感：`1 uH`。
- 5V Boost 输出侧可见 `10 uF` 去耦/储能电容。
- 控制信号：`GPIO1` 到 `GPIO12` 分别控制 12 路 VCSEL 开关。
- 监测通道：连接器上有 `PD+` / `PD-`，MCU 侧接入 `AIN0/AIN1`，可用于光电二极管反馈或光强监测。
- MCU 供电：`VDD_3V3`。
- 板上还存在 `V_1V8` 供电域。
- 每路 VCSEL 开关附近可见 `100k` 电阻，主要用于栅极/节点默认状态约束，不是可靠的 VCSEL 限流参数。

## 不能只靠这张电路图确定的参数

以下参数不能仅由电路图可靠推出：

- VCSEL 单点光功率。
- VCSEL 单点电流。
- VCSEL 阵列真实几何排布和点间距。
- 单点发散角、椭圆光斑比例、准直误差。
- 单点功率不均匀性。
- 偏振状态。
- 模式竞争、温漂、波长漂移。
- Boost 最大输出电流、瞬态纹波、调制带宽。
- MOSFET 开关上升/下降沿时间。

原因：电路图只描述连接关系和部分元件值；上述参数需要 VCSEL 发射器数据手册、BOM、驱动芯片/开关管型号、PCB 寄生参数或实测数据。

## 建议加入程序的参数层级

### 第一层：可以现在加入的板级参数

这些可以从当前电路图合理加入：

- `vcsel_channel_count = 12`
- `vcsel_supply_v = 5.0`
- `vcsel_boost_input = VBATT`
- `vcsel_boost_inductor_uh = 1.0`
- `vcsel_output_cap_uf = 10.0`
- `vcsel_has_pd_feedback = True`
- `vcsel_gpio_channels = 12`

它们主要用于界面展示、硬件说明、时序通道数量限制、功耗估计的上层框架。

### 第二层：需要 BOM / 芯片型号后才能加入

需要你再提供 BOM、驱动芯片型号、MOSFET 型号或清晰原理图标注：

- Boost 芯片型号。
- Boost 开关频率。
- Boost 最大输出电流。
- Boost 限流阈值。
- Boost 输出纹波。
- MOSFET 型号、导通电阻 `Rds_on`、栅极电荷 `Qg`。
- GPIO 驱动电压与最大驱动电流。
- VCSEL 串联限流电阻或恒流驱动参数。

这些可以接入仿真中的电流限制、脉冲上升沿、通道间电压跌落、帧间亮度波动。

### 第三层：必须通过光学规格或实测加入

需要 VCSEL 阵列数据手册、光斑标定图片或积分球/光功率计测量：

- 每个 VCSEL 点的实际 `(x, y)` 阵列坐标。
- 每点输出功率 `mW`。
- 每点发散角，例如 `theta_x/theta_y`。
- 点光斑椭圆度。
- 点间功率不均匀性。
- 波长中心值和谱宽。
- 随电流/温度变化的光功率曲线。
- 偏振方向和偏振消光比。

这些才是“真实投影光场”的核心参数，会直接影响点阵密度、阴影、曝光、EVS 事件强度和点云可靠区域。

## 接入代码的推荐步骤

1. 在 `SimState` 中新增 VCSEL 驱动板参数：
   - 通道数、供电电压、Boost 电感、输出电容、PD 反馈开关。

2. 在左侧参数区新增“VCSEL 驱动板参数”小节：
   - 先只放可确认或可手动输入的参数。
   - 不把无法确定的参数写死成“真实值”。

3. 在 `temporal_pattern()` 中引入通道响应：
   - 每个点阵列/通道乘以 `channel_gain[i]`。
   - 加入由 Boost 供电导致的全局亮度 droop。
   - 加入 GPIO/MOSFET 造成的开关边沿模糊。

4. 在 `sensor_response()` 前加入 VCSEL 光源电学模型：
   - `optical_power = f(current, duty, temperature, channel_gain)`。
   - 当前没有实测时，使用可调参数而不是假装精确。

5. 如果使用 `PD+ / PD-`：
   - 可以模拟光电反馈，让程序显示估计光强。
   - 真实闭环控制需要知道 PD responsivity、采样电阻、ADC 标定。

## 当前最缺的文件

为了把它做成真实物理参数，而不是展示级近似，建议继续提供：

- VCSEL 发射器/阵列规格书。
- VCSEL 驱动板 BOM。
- Boost 芯片型号和数据手册。
- MOSFET 型号和数据手册。
- VCSEL 阵列机械尺寸图或光斑坐标标定图。
- 每路点亮时的电流、光功率实测表。
- PD 反馈电路的采样电阻、放大器型号、ADC 标定关系。

有了这些之后，才能把“VCSEL 阵列真实排布、单点功率差异、发散角、驱动带宽、供电纹波”等参数从未接入列表中移到已接入列表。
